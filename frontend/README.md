# Frontend (Vite + React + TypeScript)

Quick start (from `frontend`):

1. `npm install`
2. `npm run dev`

This scaffold contains a simple Login and Dashboard placeholder and React Bootstrap theme (purple/black).
